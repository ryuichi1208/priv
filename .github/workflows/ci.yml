name: Release

on:
  push:

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      run: |
        A="BBB"
        echo "::group::${A}"
        ls
        pwd
        uname -a
        echo "::endgroup::"
        A="CCC"
        echo "::group::${A}"
        ls
        pwd
        uname -a
        echo "::endgroup::"
